{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:best-practices",
    ":automergeDigest",
    ":automergePatch"
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Update versioned ARGs in Dockerfiles",
      "managerFilePatterns": [
        "/(^|/)Dockerfile$/"
      ],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>[^\\s]+) depName=(?<depName>[^\\s]+)\\nARG \\w+=(?<currentValue>\\S+)"
      ]
    },
    {
      "customType": "regex",
      "description": "Update go install versions in Dockerfiles",
      "managerFilePatterns": [
        "/(^|/)Dockerfile$/"
      ],
      "matchStrings": [
        "go install (?<depName>[^@]+)@(?<currentValue>v\\S+)"
      ],
      "datasourceTemplate": "go"
    }
  ],
  "packageRules": [
    {
      "description": "Codex releases are tagged rust-v<semver>",
      "matchDepNames": ["openai/codex"],
      "extractVersion": "^rust-v(?<version>.+)$"
    },
    {
      "description": "Update checksums when pinned tool versions are bumped",
      "matchDepNames": ["mikefarah/yq", "dandavison/delta", "bun", "SocketDev/sfw-free", "rui314/mold", "gastownhall/beads", "j178/prek", "openai/codex", "cargo-bins/cargo-binstall", "wild-linker", "cargo-cache"],
      "postUpgradeTasks": {
        "commands": ["bash scripts/update-checksums.sh"],
        "fileFilters": ["base/Dockerfile", "rust/Dockerfile"],
        "executionMode": "update"
      }
    }
  ]
}
